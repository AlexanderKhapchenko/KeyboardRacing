export enum CommentatorAction {
    HELLO = 'hello',
    BEFORE_GAME = 'before_game',
    EVERY_30_SEC = 'every_30_sec',
    SYMBOL_30_TO_FINISH = 'symbol_30_to_finish',
    USER_ON_FINISH = 'user_on_finish',
    GAME_OVER = 'game_over',
    ANYTHING = 'anything',
}